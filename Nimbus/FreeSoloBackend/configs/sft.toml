model = "Qwen/Qwen3.5-4B"
algorithm = "sft"
seed = 42

# Main intent-parser run over the full generated dataset (run AFTER smoke).
[environment]
id = "nimbus/voice-drone-objective"

[train]
epochs = 1
max_examples = 12000
lora_rank = 32
lora_alpha = 64
max_context_tokens = 1024
max_steps = 250
