model = "Qwen/Qwen3.5-9B"
algorithm = "grpo"
seed = 42

# init_from_adapter = "<sft-run-id>"

[environment]
id = "nimbus/voice-drone-objective"

[train]
epochs = 1
max_examples = 4000
group_size = 4
max_completion_tokens = 512
structured_outputs = { json_object = true }
